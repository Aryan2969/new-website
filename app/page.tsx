'use client'
import { motion, useMotionValue, useSpring, useTransform } from 'framer-motion'
import { ArrowUpRight, Search, ShoppingBag, Sparkles, Menu, X, Heart, Zap } from 'lucide-react'
import { useState } from 'react'

const products=[['Creator OS','A complete workspace for turning ideas into products.','₹799','Productivity'],['Studio Kit','Premium templates for creators who care about the details.','₹499','Templates'],['Launch Manual','A practical playbook for your first digital product.','₹299','Guides'],['Motion Pack','Cinematic motion assets for ambitious launches.','₹699','Motion'],['Brand Vault','A sharp identity system for modern creators.','₹999','Design'],['Focus Notes','A minimal system for deep creative work.','₹249','Productivity']]
const creators=[['M','Maya Chen','Design systems & digital craft','12 products'],['A','Arjun Rao','Build better. Ship faster.','8 products'],['S','Sara Kim','Creative business, simplified.','21 products']]

function TiltCard({children}:{children:React.ReactNode}){const x=useMotionValue(0),y=useMotionValue(0);const rx=useTransform(y,[-100,100],[6,-6]),ry=useTransform(x,[-100,100],[-6,6]);return <motion.div style={{rotateX:rx,rotateY:ry,transformPerspective:900}} onMouseMove={e=>{const r=e.currentTarget.getBoundingClientRect();x.set(e.clientX-r.left-r.width/2);y.set(e.clientY-r.top-r.height/2)}} onMouseLeave={()=>{x.set(0);y.set(0)}}>{children}</motion.div>}
function Orb(){return <motion.div className="relative h-56 w-56 md:h-72 md:w-72" animate={{y:[0,-14,0],rotate:[0,4,0]}} transition={{duration:6,repeat:Infinity,ease:'easeInOut'}}><div className="absolute inset-8 rounded-[30%] bg-gradient-to-br from-white via-zinc-200 to-red-600 shadow-[0_0_100px_rgba(239,68,68,.35)] rotate-12"/><div className="absolute inset-14 rounded-full bg-yellow-300 mix-blend-screen blur-xl opacity-70"/><div className="absolute inset-0 rounded-full border border-white/20"/></motion.div>}
export default function Home(){const [menu,setMenu]=useState(false);const [query,setQuery]=useState('');const [category,setCategory]=useState('All');const [liked,setLiked]=useState<string[]>([]);const [cart,setCart]=useState<string[]>([]);const [toast,setToast]=useState('');const categories=['All','Productivity','Templates','Guides','Motion','Design'];const filtered=products.filter(p=>(category==='All'||p[3]===category)&&p.slice(0,2).join(' ').toLowerCase().includes(query.toLowerCase()));const notify=(m:string)=>{setToast(m);setTimeout(()=>setToast(''),2200)};return <main className="min-h-screen overflow-hidden selection:bg-red-500 selection:text-white"><section className="relative grid-bg"><div className="glow" style={{top:-180,right:-100}}/><nav className="shell flex h-24 items-center justify-between"><a className="display text-xl font-bold tracking-tight" href="#top">makelane<span className="accent-red">.</span></a><div className="hidden md:flex gap-8 text-sm text-zinc-400"><a href="#explore">Explore</a><a href="#creators">Creators</a><a href="#sell">Sell on Makelane</a></div><div className="flex items-center gap-3"><button aria-label="Cart" onClick={()=>notify(`${cart.length} item${cart.length===1?'':'s'} in your bag`)} className="btn btn-ghost relative p-3"><ShoppingBag size={17}/>{cart.length>0&&<span className="absolute -right-1 -top-1 flex h-5 w-5 items-center justify-center rounded-full bg-red-500 text-[10px]">{cart.length}</span>}</button><a className="btn btn-ghost hidden text-sm md:inline-flex" href="#login">Sign in</a><a className="btn btn-primary hidden text-sm md:inline-flex" href="#sell">Start creating</a><button onClick={()=>setMenu(!menu)} className="btn btn-ghost p-3 md:hidden" aria-label="Menu">{menu?<X size={18}/>:<Menu size={18}/>}</button></div></nav>{menu&&<div className="shell pb-5 md:hidden"><div className="card grid gap-2 p-3 text-sm"><a className="rounded-xl p-3 hover:bg-white/5" href="#explore" onClick={()=>setMenu(false)}>Explore</a><a className="rounded-xl p-3 hover:bg-white/5" href="#creators" onClick={()=>setMenu(false)}>Creators</a><a className="rounded-xl p-3 hover:bg-white/5" href="#sell" onClick={()=>setMenu(false)}>Sell on Makelane</a></div></div>}<div id="top" className="shell relative flex flex-col items-center py-20 md:flex-row md:py-32"><div className="max-w-3xl"><motion.div initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} className="mb-7 inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-4 py-2 text-xs text-zinc-300"><Sparkles size={13} className="accent-yellow"/> A new home for independent creators</motion.div><motion.h1 initial={{opacity:0,y:30}} animate={{opacity:1,y:0}} transition={{delay:.1}} className="display text-6xl font-semibold leading-[.92] tracking-[-.055em] md:text-8xl">Make something<br/><span className="accent-red">worth buying.</span></motion.h1><motion.p initial={{opacity:0}} animate={{opacity:1}} transition={{delay:.25}} className="mt-8 max-w-xl text-lg leading-8 text-zinc-400">Makelane gives creators a beautiful place to turn what they know, make, and love into products people want.</motion.p><div className="mt-9 flex flex-wrap gap-3"><motion.a whileHover={{scale:1.04}} whileTap={{scale:.97}} className="btn btn-primary" href="#sell">Start creating <ArrowUpRight size={17}/></motion.a><a className="btn btn-ghost" href="#explore">Explore products</a></div></div><div className="flex flex-1 items-center justify-center py-12 md:py-0"><TiltCard><Orb/></TiltCard></div></div></section><section id="explore" className="shell py-28"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p className="text-xs uppercase tracking-[.25em] accent-yellow">Discover</p><h2 className="display mt-3 text-4xl font-semibold tracking-tight">Things worth making.</h2></div><div className="flex flex-wrap gap-2">{categories.map(c=><button key={c} onClick={()=>setCategory(c)} className={`rounded-full border px-4 py-2 text-xs transition ${category===c?'border-red-500 bg-red-500 text-white':'border-white/10 text-zinc-400 hover:bg-white/5'}`}>{c}</button>)}</div></div><div className="mt-8 flex items-center gap-3 rounded-2xl border border-white/10 bg-white/[.03] px-4 py-3"><Search size={17} className="text-zinc-500"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search products..." className="w-full bg-transparent text-sm outline-none placeholder:text-zinc-600"/></div><div className="mt-8 grid gap-5 md:grid-cols-3">{filtered.map(([name,desc,price,cat],i)=><TiltCard key={name}><article className="card group overflow-hidden"><motion.div whileHover={{scale:1.03}} className="aspect-[1.15] bg-gradient-to-br from-zinc-800 via-zinc-950 to-red-950/50 p-6"><div className="flex h-full items-end justify-between"><span className="rounded-full bg-black/40 px-3 py-1 text-xs text-zinc-300 backdrop-blur">{cat}</span><button onClick={()=>setLiked(v=>v.includes(name)?v.filter(x=>x!==name):[...v,name])} className="rounded-full bg-black/40 p-2 backdrop-blur" aria-label="Like"><Heart size={15} fill={liked.includes(name)?'currentColor':'none'} className={liked.includes(name)?'text-red-400':''}/></button></div></motion.div><div className="p-6"><div className="flex items-start justify-between gap-4"><div><h3 className="display text-xl font-semibold">{name}</h3><p className="mt-2 text-sm leading-6 text-zinc-500">{desc}</p></div><span className="font-semibold accent-yellow">{price}</span></div><button onClick={()=>{setCart(v=>v.includes(name)?v:[...v,name]);notify(`${name} added to bag`)}} className="btn btn-ghost mt-6 w-full text-sm">Add to bag</button></div></article></TiltCard>)}</div>{filtered.length===0&&<div className="py-20 text-center text-zinc-500">No products found. Try another search.</div>}</section><section id="creators" className="border-y border-white/5 bg-white/[.02] py-28"><div className="shell"><p className="text-xs uppercase tracking-[.25em] accent-red">The people behind it</p><h2 className="display mt-3 max-w-2xl text-4xl font-semibold tracking-tight md:text-5xl">Made by people,<br/><span className="text-zinc-500">not corporations.</span></h2><div className="mt-12 grid gap-4 md:grid-cols-3">{creators.map(([initial,name,bio,count],i)=><motion.div whileHover={{y:-7}} transition={{type:'spring',stiffness:300}} className="card p-6" key={name}><div className="flex items-center gap-4"><div className={`flex h-12 w-12 items-center justify-center rounded-full font-semibold ${i%2===0?'bg-red-500 text-white':'bg-yellow-400 text-black'}`}>{initial}</div><div><h3 className="font-semibold">{name}</h3><p className="text-xs text-zinc-500">{count}</p></div></div><p className="mt-8 text-sm leading-6 text-zinc-400">{bio}</p><a className="mt-6 inline-block text-sm text-zinc-200" href="#">Visit store →</a></motion.div>)}</div></div></section><section id="sell" className="shell py-32"><motion.div whileInView={{opacity:1,y:0}} initial={{opacity:0,y:30}} viewport={{once:true}} className="relative overflow-hidden rounded-[32px] border border-white/10 bg-gradient-to-br from-white/[.08] to-red-500/[.06] p-8 md:p-16"><div className="glow" style={{right:-240,bottom:-300}}/><div className="relative max-w-2xl"><p className="text-xs uppercase tracking-[.25em] accent-yellow">For creators</p><h2 className="display mt-4 text-5xl font-semibold tracking-tight md:text-7xl">You make it.<br/>We give it a place to go.</h2><p className="mt-7 text-zinc-400">Launch your store, sell digital products, and build something that belongs to you.</p><motion.a whileHover={{scale:1.04}} className="btn btn-primary mt-9" href="#signup">Create your store <Zap size={16}/></motion.a></div></motion.div></section><footer className="border-t border-white/5"><div className="shell flex flex-col gap-5 py-10 text-sm text-zinc-500 md:flex-row md:items-center md:justify-between"><div className="display font-semibold text-zinc-200">makelane<span className="accent-red">.</span></div><div>© 2026 Makelane. Made for people who make.</div></div></footer>{toast&&<motion.div initial={{y:30,opacity:0}} animate={{y:0,opacity:1}} exit={{y:30,opacity:0}} className="fixed bottom-6 left-1/2 z-50 -translate-x-1/2 rounded-full border border-white/10 bg-zinc-900/90 px-5 py-3 text-sm shadow-2xl backdrop-blur">{toast}</motion.div>}</main>}
